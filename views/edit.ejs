<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Crear nueva lista</title>
  </head>
  <body>
    <a href="/">HOME</a>
    <form action="/mylists/<%= lista2._id%>?_method=PUT" method="post">
      <div>
        <label for="lnombre">Nombre de la lista</label>
        <input
          type="text"
          name="dtlista[nombre]"
          id="lnombre"
          required
          placeholder="Nombre de la lista"
          value="<%= lista2.nombre %>"
        />
      </div>
      <div>
        <label for="ldesc">Descripcion</label>
        <br />
        <textarea
          name="dtlista[descripcion]"
          id="ldesc"
          cols="30"
          rows="10"
        ><%= lista2.descripcion%></textarea>
      </div>
      <div>
        <label for="lprio">Prioridad</label>
        <select name="dtlista[prioridad]" id="lprio">
            <% for(let categoria of prio){ %>
                <option value='<%= categoria%>' <%= (lista2.prioridad === categoria ? 'selected': '')%>><%= categoria%></option> 
                <% } %>  
        </select>
      </div>
      <button>Guardar Cambios</button>
    </form>
    <a href="/mylists">Regresar a mi listas</a>
  </body>
</html>
